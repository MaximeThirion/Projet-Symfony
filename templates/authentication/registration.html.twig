{% extends 'public.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div class="container">
        <h1>{{ title }}</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
{% endblock %}
